# `rcon-client`

Internal Rust crate that wraps the upstream `rcon` crate with reconnect, heartbeat, and BattlEye-RCON-specific behaviour for the Conan Exiles Enhanced wire protocol.

Currently a stub — implementation lands in a follow-up PR.
