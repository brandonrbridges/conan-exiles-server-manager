{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions granted to the main app window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
